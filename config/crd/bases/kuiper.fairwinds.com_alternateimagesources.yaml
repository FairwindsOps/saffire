
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.5
  creationTimestamp: null
  name: alternateimagesources.kuiper.fairwinds.com
spec:
  group: kuiper.fairwinds.com
  names:
    kind: AlternateImageSource
    listKind: AlternateImageSourceList
    plural: alternateimagesources
    singular: alternateimagesource
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: AlternateImageSource is the Schema for the alternateimagesources
        API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: AlternateImageSourceSpec defines the desired state of AlternateImageSource
          properties:
            imageSourceReplacements:
              items:
                description: ImageSourceReplacement is a single replacement
                properties:
                  initialRepository:
                    description: 'InitialRepository is the repository that you deploy
                      with. For example: quay.io/fairwinds/polaris'
                    type: string
                  replacementRepositories:
                    description: ReplacementRepositories is the list of potential
                      1:1 replacements
                    items:
                      type: string
                    type: array
                  targets:
                    description: Targets is a list of objects you want to target for
                      replacement of the image in the event of an ImagePullError
                    items:
                      description: Target is a target for image replacement
                      properties:
                        name:
                          type: string
                        type:
                          description: GroupKind specifies a Group and a Kind, but
                            does not force a version.  This is useful for identifying
                            concepts during lookup stages without having partially
                            valid types
                          properties:
                            group:
                              type: string
                            kind:
                              type: string
                          required:
                          - group
                          - kind
                          type: object
                      required:
                      - name
                      - type
                      type: object
                    type: array
                required:
                - initialRepository
                - replacementRepositories
                - targets
                type: object
              type: array
          required:
          - imageSourceReplacements
          type: object
        status:
          description: AlternateImageSourceStatus defines the observed state of AlternateImageSource
          properties:
            activated:
              description: Activated status inidicates if a replacement has been used
              type: boolean
            observedGeneration:
              format: int64
              type: integer
            targets:
              description: 'Targets is a list of objects that the LabelSelector has
                matched TODO: Find the right type'
              items:
                type: string
              type: array
          required:
          - observedGeneration
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
