## DO NOT EDIT - Managed by Terraform
# yaml-language-server: $$schema=https://goreleaser.com/static/schema.json

version: 2
project_name: saffire
brews:
  - name: saffire
    goarm: 6
    repository:
      owner: FairwindsOps
      name: homebrew-tap
    directory: Formula
    description: Kubernetes AlternateImageSource controller
    url_template: "https://github.com/FairwindsOps/saffire/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
    test: |
      assert_match "flag provided but not defined", shell_output("#{bin}/saffire -not-a-goreleaser-test-flag 2>&1", 2)
before:
  hooks:
    - go mod tidy
builds:
  - id: saffire
    main: main.go
    ldflags:
      - -X main.version={{.Version}} -X main.commit={{.Commit}} -s -w
    goarch:
      - amd64
      - arm
      - arm64
    env:
      - CGO_ENABLED=0
      - GO111MODULE=on
    goos:
      - linux
      - darwin
    goarm:
      - 6
      - 7
release:
  disable: '{{ eq (envOrDefault "GORELEASER_SKIP_RELEASE" "false") "true" }}'
  prerelease: auto
  github:
    owner: FairwindsOps
    name: saffire
  footer: |
    You can verify the signatures of both the checksums.txt file and the published docker images using [cosign](https://github.com/sigstore/cosign).

    ```bash
    cosign verify-blob checksums.txt --bundle=checksums.txt.sigstore.json --key https://artifacts.fairwinds.com/cosign-p256.pub
    ```

    ```bash
    cosign verify us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .Tag }} --key https://artifacts.fairwinds.com/cosign-p256.pub
    ```
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
checksum:
  name_template: "checksums.txt"
signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - "sign-blob"
      - "--key=hashivault://cosign-p256"
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: all
docker_signs:
  - artifacts: all
    args: ["sign", "--key=hashivault://cosign-p256", "us-docker.pkg.dev/fairwinds-ops/oss/saffire@${digest}", "-r", "--yes"]
dockers:
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .FullCommit }}-amd64"
        - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .Tag }}-amd64"
     use: buildx
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/amd64"
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .FullCommit }}-arm64v8"
        - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .Tag }}-arm64v8"
     use: buildx
     goarch: arm64
     goos: linux
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/arm64/v8"
   - image_templates:
        - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .FullCommit }}-armv7"
        - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .Tag }}-armv7"
     use: buildx
     goarch: arm
     goarm: 7
     goos: linux
     dockerfile: Dockerfile
     build_flag_templates:
        - "--platform=linux/arm/v7"
docker_manifests:
  - name_template: us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .Tag }}
    image_templates:
      - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .Tag }}-amd64"
      - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .Tag }}-arm64v8"
      - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .Tag }}-armv7"
  - name_template: us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .FullCommit }}
    image_templates:
      - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .FullCommit }}-amd64"
      - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .FullCommit }}-arm64v8"
      - "us-docker.pkg.dev/fairwinds-ops/oss/saffire:{{ .FullCommit }}-armv7"
